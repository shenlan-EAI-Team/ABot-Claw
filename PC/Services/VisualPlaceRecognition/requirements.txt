--extra-index-url https://download.pytorch.org/whl/cu124

fastapi>=0.116.0,<1.0
uvicorn[standard]>=0.35.0,<1.0
pydantic>=2.11.0,<3.0
numpy>=1.26.0,<3.0
Pillow>=10.4.0,<13.0
httpx>=0.27.0,<1.0
python-multipart>=0.0.20,<1.0
faiss-cpu>=1.8.0,<2.0

# Match the repository's CUDA 12.4 / YOLO runtime family. FAISS remains CPU-only;
# only SALAD descriptor extraction uses CUDA when VPR_DEVICE resolves to cuda.
torch==2.5.1+cu124
torchvision==0.20.1+cu124
pytorch-lightning>=2.1.2,<3.0
pytorch-metric-learning>=2.3.0,<3.0
torchmetrics>=1.2.0,<2.0
timm>=0.9.12,<2.0

# Kept here so a service checkout can run its tests without a second lock file.
pytest>=8.0.0,<10.0
pytest-asyncio>=0.24.0,<2.0

